@require '../../vars'

.z-block
  display: flex
  flex-direction: column
  background: getColor('$bgColor')
  border-radius: 6px
  border: 1px solid getColor('$bgText6')
  padding: 20px

  > .name
    font-subtitle1(medium)
